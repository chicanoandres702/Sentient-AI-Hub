
# Project Wiring Summary

This file is auto-generated by the Refactor Wiring Agent to show all agent, module, and feature interconnections for maximum clarity and traceability.

- All agents must be explicitly imported and connected to their consumers.
- No orphaned modules, features, or exports allowed.
- Use this summary to audit and refactor project wiring as needed.

## AIDDE Protocols & Wiring

### Boot Sequence
Executed by `context-manager` agent at session start. Authenticates, hydrates memory, flags orphans/stale branches, and prepares ready state.

### AI Safety Protocols
Disambiguation, idempotency, one-task-per-turn, rollback, stale branch, context window, credential hygiene, conflict resolution, contract change warning. All agents must enforce these rules.

### CI/CD Gates
Every PR must pass 8 gates: orchestration, parent hierarchy, template, static analysis, wiring, test, complexity, security. The orchestrator and subagents validate each gate.

### Traceability Engine
All issues, commits, and files must use strict templates and trace headers. No orphaned code or ambiguous wiring allowed.

### Universal Contract
Type safety enforced by type-enforcer agent. All models imported from centralized directory. Contract changes require explicit confirmation and atomic updates.

### Git Orchestrator
Provisions milestones, issues, branches, and PRs. Stashes/switches context, enforces hierarchy, and logs all actions for audit.

### Refactor Wiring Agent
Scans for orphaned files/modules, generates wiring map, refactors imports/exports, and outputs this summary for compliance.

---

> For full details, see `/docs/aidde/` modules and agent source files.
